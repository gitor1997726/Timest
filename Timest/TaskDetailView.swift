import SwiftUI

struct TaskDetailView: View {
    var body: some View {
        Text("タスク詳細画面")
            .font(.largeTitle)
            .foregroundColor(.white)
            .background(Color.black)
    }
}
